# Minimal editor definition — prevents Eclipse Che from injecting VS Code.
# The devfile's own container provides the ttyd web terminal (type: main).

inline:
  schemaVersion: 2.1.0
  metadata:
    name: empty-editor
